/**
 * endpoints.js
 *
 * Add your API endpoint constants here as your backend is designed.
 * Pattern: ENDPOINTS.FEATURE.ACTION
 *
 * Example:
 *   export const ENDPOINTS = {
 *     AUTH: {
 *       LOGIN:  '/auth/login',
 *       ME:     '/auth/me',
 *       LOGOUT: '/auth/logout',
 *     },
 *     VEHICLES: {
 *       LIST:   '/vehicles',
 *       DETAIL: (id) => `/vehicles/${id}`,
 *     },
 *   }
 *
 * Keep paths relative to the axios baseURL (no domain prefix).
 */

export const ENDPOINTS = {
  // Add endpoints here as the backend routes are confirmed
}
